let nome = prompt ('fale seu nome :')
let sexo = prompt ('fale seu sexo :')
let estadoc = prompt ('fale seu estado civl :')

if(sexo.toUpperCase() === 'F' && estadoc.toUpperCase()==='CASADA') {
    prompt('Quantos anos de casamento?:')
} 